# Tadarruj Phase 1 + 2 — requirements.txt
# Install: pip install -r requirements.txt
# Then on GPU: pip install unsloth

# ── Core ML ──────────────────────────────────────────────────────
torch==2.10.0
transformers==5.5.0
datasets==4.3.0
accelerate==1.13.0
peft==0.19.1
trl==0.24.0
bitsandbytes==0.49.2

# ── Agent ────────────────────────────────────────────────────────
langgraph==0.2.0
langchain==0.2.16
langchain-core==0.2.38
pydantic==2.8.2

# ── RAG ──────────────────────────────────────────────────────────
chromadb==0.5.3
sentence-transformers==3.0.1

# ── App and Evaluation ───────────────────────────────────────────
streamlit==1.37.0
openai==1.40.0
httpx==0.27.0
python-dotenv==1.0.1

# ── Note: install unsloth separately on GPU studio ───────────────
# pip install unsloth